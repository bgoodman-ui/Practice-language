# Practice-language
Making a coding language for practice haven't decided on a theme yet

--import antlr
--make .g file contents
--make math operations all one function call
--make function call logic

math operations will be called as follows:

addition
    add(number,number);
for other members of PEMDAS just change add for other operations words those being
add,minus,mult,div,mod,exp

concatenations is called as 
    merge[thing#thing];

loop syntax

if equivalent
    case
for equivalent
    each
while equivalent
    when

delete equivalent
    remove